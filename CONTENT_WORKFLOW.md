# Notion 콘텐츠 갱신 절차

1. 연결된 Notion의 `기술 질문` 페이지를 Markdown 또는 텍스트로 가져옵니다.
2. `node scripts/import-notion-content.mjs <파일>`로 학습 데이터를 반영합니다.
3. `npm run quiz:scaffold -- "새 질문 제목이란?"`으로 차단된 퀴즈 초안을 만듭니다.
4. `quiz-concepts.json`에 최소 3개의 사실 문장과 4개의 정교한 오개념 문장, 각각의 해설을 작성합니다.
   - 선택지는 질문과 같은 개념 범위 안에서 서로 경쟁할 수 있어야 합니다.
   - 오답은 단순 부정이나 다른 개념 끼워 넣기가 아니라, 실제로 혼동하기 쉬운 전제·책임·실행 순서를 바꿔 구성합니다.
   - 해설은 최소 35자 이상으로 작성하고, 무엇이 잘못됐는지와 실제 동작이 무엇인지 함께 설명합니다. 이해에 필요한 경우 실행 순서나 짧은 예시를 덧붙입니다.
   - 정답 해설은 선택지 문장을 포함한 독립적인 설명이어야 하며, 선택지만 읽어도 근거를 이해할 수 있어야 합니다.
   - 정답과 오답의 길이와 문체를 비슷하게 맞춰 정답이 형식만으로 드러나지 않게 합니다.
   - 신규 퀴즈는 `qualityVersion: 2`를 사용합니다. 모든 해설은 60자 이상, 최소 두 문장으로 작성해야 빌드를 통과합니다.
   - 학습 설명만으로 정확한 선택지를 만들기 어렵다면 필요한 학습 내용을 먼저 보강합니다. 문제 수만 늘리기 위한 학습 항목은 추가하지 않습니다.
   - React, Next.js, 브라우저 API처럼 동작이 바뀔 수 있는 내용은 공식 문서를 확인한 뒤 승인합니다.
5. `reviewStatus`를 `approved`로 지정합니다. 초안 상태에서는 앱에 포함되지 않습니다.
6. 기존 학습 설명을 수정했다면 퀴즈도 다시 검토한 뒤 `npm run quiz:approve-source -- "질문 제목"`을 실행합니다.
7. `npm run content:approve`로 퀴즈 품질 검사와 콘텐츠 승인을 함께 수행합니다.
8. `npm run quality`와 `npm run build`가 통과한 결과만 배포합니다.

새 학습 항목에 검수된 퀴즈가 없거나, 학습 설명이 바뀌었는데 연결된 퀴즈를 다시 검수하지 않으면 빌드가 실패합니다.
